/* ============================================================
   TESTIMONIALS

   Empty on purpose. Everything else on this site can be written;
   a client's words cannot — inventing a quote and signing a real
   name to it is the one thing that would make the rest of the
   page untrustworthy.

   Add entries as they come in and the section appears on the page
   by itself. Two or three good ones are plenty:

     {
       quote: "He spent the whole morning with us and we barely noticed him.",
       name: "Anisha & Bibek",
       context: "Wedding · Dharan · 2026"
     }

   `placeholder: true` on an entry adds a visible notice to the
   section, so a draft can never quietly pass as a real quote.
   ============================================================ */
export const testimonials = [];
